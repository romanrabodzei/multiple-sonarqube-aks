# .Synopsis
#     All resources for this instance (StatefulSet, Service, Secrets, SPC) live here.
#     Isolation: each instance has its own namespace so RBAC, secrets, and resource
#     quotas are scoped independently.
#
# .NOTES
#     Author  : Roman Rabodzei
#     Version    : 1.0.260410
#
#             _
#         .__(.)<  (MEOW)
#          \___)
#  ~~~~~~~~~~~~~~~~~~~~~~~~

apiVersion: v1
kind: Namespace
metadata:
  name: sonarqube-server-two
  labels:
    app.kubernetes.io/name: sonarqube
    app.kubernetes.io/instance: sonarqube-server-two
